type Vote {
  id: ID!
  label: String!
  count: Int!
}

type VoteEvent {
  voterName: String!
  optionLabel: String!
  vote: Vote!
}

type Query {
  getVotes: [Vote!]!
}

type Mutation {
  addVote(id: ID!, voterName: String!): Vote!
}

type Subscription {
  voteUpdated: VoteEvent!
}
